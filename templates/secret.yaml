apiVersion: v1
kind: Secret

metadata:
  name: {{ .Values.secret.name }}

type: {{ .Values.secret.type }}

stringData:
{{- range $key, $value := .Values.secret.data }}
  {{ $key }}: {{ $value }}
{{- end }}
